[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build || true", "which ffmpeg || echo 'Warning: ffmpeg not found in PATH'"]

[start]
cmd = "npm start"

