# 🚀 Інструкція з налаштування

Покрокова інструкція для запуску **Смачно.AI**.

## Крок 1: Створення Telegram бота

1. Відкрийте Telegram та знайдіть [@BotFather](https://t.me/BotFather)
2. Відправте команду `/newbot`
3. Введіть назву бота (наприклад: "Смачно.AI")
4. Введіть username бота (наприклад: `smachno_ai_bot`)
5. Скопіюйте отриманий токен - він знадобиться для `.env`

## Крок 2: Отримання OpenAI API ключа

1. Зареєструйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдіть в [API Keys](https://platform.openai.com/api-keys)
3. Створіть новий API ключ
4. Скопіюйте ключ - він знадобиться для `.env`

**Важливо:** Переконайтесь, що у вас є кошти на рахунку OpenAI для використання DALL·E 3 та GPT-4o-mini.

## Крок 3: Налаштування Supabase

### 3.1 Створення проєкту

1. Зареєструйтесь на [supabase.com](https://supabase.com)
2. Створіть новий проєкт
3. Запам'ятайте:
   - **Project URL** (Supabase URL)
   - **anon/public key** (Supabase Key)

### 3.2 Створення таблиць

1. Перейдіть в **SQL Editor** в панелі Supabase
2. Відкрийте файл `src/db/schema.sql`
3. Скопіюйте весь SQL код
4. Вставте в SQL Editor та виконайте (Run)

### 3.3 Створення Storage Bucket

1. Перейдіть в **Storage** в панелі Supabase
2. Натисніть **New bucket**
3. Назва: `creatives`
4. Встановіть **Public bucket** = `true`
5. Створіть bucket

### 3.4 Налаштування політик Storage (ОБОВ'ЯЗКОВО!)

**Важливо:** Без цих політик завантаження файлів не працюватиме!

1. Перейдіть в **SQL Editor** в панелі Supabase
2. Виконайте наступні SQL-запити:

```sql
-- Дозволити публічне читання файлів
CREATE POLICY "Public Read Access" ON storage.objects
FOR SELECT
USING (bucket_id = 'creatives');

-- Дозволити завантаження файлів (INSERT)
CREATE POLICY "Public Upload Access" ON storage.objects
FOR INSERT
WITH CHECK (bucket_id = 'creatives');

-- Дозволити оновлення файлів (якщо потрібно)
CREATE POLICY "Public Update Access" ON storage.objects
FOR UPDATE
USING (bucket_id = 'creatives')
WITH CHECK (bucket_id = 'creatives');
```

**Альтернативний варіант (якщо використовуєте Service Role Key):**

Якщо ви хочете використовувати Service Role Key замість anon key для завантаження файлів, оновіть `.env`:

```env
SUPABASE_KEY=your_service_role_key_here  # Замість anon key
```

⚠️ **Увага:** Service Role Key обходить RLS, тому використовуйте його тільки на сервері, ніколи не публікуйте в клієнтському коді!

## Крок 4: Налаштування WayForPay

Якщо плануєте використовувати платіжну систему:

1. Зареєструйтесь на [wayforpay.com](https://wayforpay.com)
2. Створіть merchant account
3. Отримайте дані для інтеграції:

### Де знайти дані в адмін-панелі WayForPay:

#### 1. Merchant Account та Secret Key:

1. **Увійдіть в особистий кабінет** на [cabinet.wayforpay.com](https://cabinet.wayforpay.com)
2. **Перейдіть в розділ "Налаштування магазину"** або **"Магазини"**
3. **Виберіть ваш магазин** зі списку
4. **Прокрутіть сторінку вниз до секції "Реквізити мерчанта"** або **"API налаштування"**
5. Там ви знайдете:
   - **Merchant Account** (або **Merchant Login**) - це ваш ID магазину (наприклад: `t_me_6d523`)
   - **Merchant Secret Key** (або **Secret Key**) - секретний ключ (зазвичай 40 символів)

**Примітка:** Якщо не бачите Secret Key:

- Можливо, він прихований - натисніть кнопку "Показати" або "Розкрити"
- Або потрібно згенерувати новий ключ (кнопка "Згенерувати" або "Створити")

#### 2. Merchant Domain Name:

**ВАЖЛИВО:** `MERCHANT_DOMAIN_NAME` - це **ДОМЕН**, а не merchant account!

- Це має бути домен вашого сайту/сервера (наприклад: `smachnoai-production.up.railway.app`)
- Або ваш власний домен, якщо він налаштований
- **НЕ** використовуйте merchant account (типу `t_me_6d523`) як domain name!

### Налаштування вебхуків в адмін-панелі WayForPay:

1. **Увійдіть в особистий кабінет** на [wayforpay.com](https://wayforpay.com)
2. **Оберіть магазин** (якщо у вас кілька магазинів)
3. **Перейдіть в розділ "Налаштування"** (Settings) або "Налаштування магазину"
4. **Знайдіть розділ "Повідомлення"** або "Notifications" або "Webhooks"
5. **Введіть Service URL (webhook):**
   ```
   https://your-domain.com/payment/webhook
   ```
   Це URL, на який WayForPay буде надсилати POST-запити про статус платежів
6. **Введіть Return URL (redirect):**
   ```
   https://your-domain.com/payment/callback
   ```
   Це URL, на який користувач буде перенаправлений після оплати
7. **Збережіть зміни**

**Важливо:**

- Service URL повинен бути доступний з інтернету (HTTPS)
- URL повинен відповідати на POST-запити
- Для тестування можна використовувати тестові дані від WayForPay
- Перевірте, що ваш сервер може приймати POST-запити на `/payment/webhook`

**Якщо не знайдете налаштування:**

- Шукайте в розділах: "API", "Інтеграція", "Технічні налаштування"
- Або в документації WayForPay: [help.wayforpay.com](https://help.wayforpay.com/)
- Можна також вказати Service URL при створенні кожного платежу (вже реалізовано в коді)

## Крок 5: Налаштування проєкту

### 5.1 Встановлення залежностей

```bash
npm install
```

### 5.2 Створення .env файлу

```bash
cp .env.example .env
```

### 5.3 Заповнення .env

Відкрийте `.env` та заповніть всі значення:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
OPENAI_API_KEY=sk-...
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
WAYFORPAY_MERCHANT_ACCOUNT=your_merchant_account
WAYFORPAY_SECRET_KEY=your_secret_key
MERCHANT_DOMAIN_NAME=your-domain.com
PAYMENT_AMOUNT=30
PAYMENT_CURRENCY=UAH
ADMIN_USER_IDS=123456789
APP_URL=https://your-domain.com
```

**Як знайти свій Telegram ID для ADMIN_USER_IDS:**

- Напишіть [@userinfobot](https://t.me/userinfobot) в Telegram
- Скопіюйте ваш ID

## Крок 6: Запуск бота

### Локальний запуск (розробка)

```bash
npm run dev
```

### Продакшн запуск

```bash
npm start
```

Якщо все налаштовано правильно, ви побачите:

```
🤖 Запуск бота...
✅ Бот запущено успішно!
```

## Крок 7: Тестування

1. Відкрийте свого бота в Telegram
2. Відправте `/start`
3. Надішліть фото виробу або текстовий опис
4. Перевірте, чи генеруються креативи

## Крок 8: Деплой (опціонально)

### Railway

1. Створіть проєкт на [railway.app](https://railway.app)
2. Підключіть GitHub репозиторій
3. Додайте всі змінні оточення з `.env`
4. Railway автоматично визначить `package.json` та запустить бота

## Налаштування вебхуків для платежів

✅ **Webhook сервер тепер інтегрований в основний процес бота!**

При запуску `npm start` автоматично запускаються:

- Telegram бот
- Webhook сервер для обробки платежів WayForPay

**Для Railway:** Див. детальну інструкцію в `RAILWAY_SETUP.md`

## Перевірка роботи

- ✅ Бот відповідає на `/start`
- ✅ Генеруються зображення з фото
- ✅ Генеруються зображення з тексту
- ✅ Створюються підписи українською
- ✅ Команда `/my_creatives` показує галерею
- ✅ Адмін-команди працюють (`/stats`)

## Вирішення проблем

### Бот не запускається

- Перевірте, чи правильно вказаний `TELEGRAM_BOT_TOKEN`
- Перевірте, чи всі залежності встановлені (`npm install`)

### Помилки з OpenAI

- Перевірте баланс на рахунку OpenAI
- Перевірте правильність API ключа
- Перевірте, чи є доступ до DALL·E 3 та GPT-4o-mini

### Помилки з Supabase

- Перевірте правильність URL та ключа
- Перевірте, чи створені всі таблиці
- Перевірте, чи створений bucket `creatives`

### Помилки з генерацією зображень

- DALL·E 3 має обмеження на кількість запитів
- Перевірте баланс OpenAI
- Спробуйте зменшити кількість зображень (1 замість 2)
